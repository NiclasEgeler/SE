name := "generator"
version := "0.1.0"
scalaVersion := "3.2.2"
// var dependencies = Seq(
//   "com.typesafe.akka" %% "akka-actor-typed" % "2.8.0",
//   "com.typesafe.akka" %% "akka-http"        % "10.5.0",
//   "com.typesafe.akka" %% "akka-stream"      % "2.8.0"
// )

// lazy val shared = ProjectRef(project in file("../shared"), "shared")
// .dependsOn(shared)
// .aggregate(shared)
// This / libraryDependencies ++= dependencies

// This / Compile / mainClass := Some("service.PersistenceRestService")

// ThisBuild / Docker / dockerExposedPorts := Seq(9091)


